{
  "name": "camp-agent-base-model",
  "version": "0.0.1",
  "description": "a model for the agent based model",
  "main": "index.js",
  "scripts": {
    "test": "vite dev",
    "test-run": "tsc -p tsconfig.json; node ./build/index.js",
    "test-run-log": "mkdir -p logs; npm run test-run > logs/$(date +'%Y%m%d-%H%M%S').txt"
  },
  "author": "",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@types/node": "^20.14.9",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0"
  }
}
